{
  "name": "logger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "webpack-dev-server --progress --hot --inline --content-base ./dist",
    "build": "cross-env NODE_ENV=production webpack --progress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "New Future",
  "license": "ISC",
  "dependencies": {
    "@aria/webjs-sdk": "^1.2.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.5",
    "ts-loader": "^2.3.7",
    "typescript": "^2.5.2",
    "webpack": "^3.6.0"
  }
}
