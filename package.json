{
  "name": "logger",
  "version": "1.1.0",
  "description": "aria logger for front end https://aria.microsoft.com",
  "main": "index.js",
  "scripts": {
    "dev": "webpack-dev-server --progress --hot --inline --content-base ./dist",
    "build": "cross-env NODE_ENV=production webpack --progress --optimize-minimize",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "New Future",
  "license": "ISC",
  "dependencies": {
    "@aria/webjs-sdk": "^1.6.2"
  },
  "devDependencies": {
    "cross-env": "^5.0.5",
    "ts-loader": "^5.3.3",
    "typescript": "^3.0.1",
    "uglifyjs-webpack-plugin": "^2.1.1",
    "webpack": "^4.17.1",
    "webpack-cli": "^3.1.0"
  }
}
